import { Scrapper, ScrapperOptions } from '../bot/scrapper/scrapper';

const findOffers = () => {
    console.log('Scrapping...');


    setTimeout(() => {
        console.log('10 offers found.')
    }, 3000)
}

findOffers()